apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    labels: front
  name: front
spec:
  replicas: 1
  selector:
    matchLabels:
      app: front
  template:
    metadata:
      labels:
        app: front
    spec:
      containers:
      - image: jorged104/front:latest
        name: front
        ports:
        - containerPort: 4000

